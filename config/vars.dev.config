{etc_dir, "./etc"}.
{log_dir, "./log"}.

{enabled_modules, "[freemobile, inotify]"}.
{lager_console_level, debug}.

% number of milliseconds between 2 reporting of exometer metrics (10 secs)
{exometer_interval, 10000}.

{redis_server, "localhost"}.
{redis_port, 6379}.

% mkpasswd vars
{mkpasswd_libs_path, "../_build/default/lib"}.
