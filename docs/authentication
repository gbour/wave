

user --> device

user
	. userid (autoset)
	. username
	. password (ui connection. HASHED)

	...

	key: "user:"+userid

device
	deviceid
		user can enter a custom one (may be set hardly in the device)		
		user having several identical devices ??

		/!\ limited to 23 chars in 3.1 version

	username
		suggère user.username/device.username

	password	
		autogenerated / we store the hashed version

	fixed ip
		check de l'ip à la connexion


	key : "device:"+userid+"/"+deviceid

	un device ce logge: infos= deviceid, username

	=> deviceid = {username, hash(password), ipaddr, max-client-keepalive, srv-keepalive} 
